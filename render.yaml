# Render Blueprint: New → Blueprint → репозиторий.
#
# Если сервис уже создан как Node: Settings → удалить сервис или создать новый Web Service
# с Environment = Docker (не Native Node). Build/Start command оставить пустыми.
#
# Либо подключи этот blueprint заново.

services:
  - type: web
    name: attendance-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
